import ract from 'react'
import { SafeAreaView } from 'react-native'
import { callurl } from '../../apifile'

function CRGRAllocation({navigation,route}){
    return(
<SafeAreaView>

</SafeAreaView>
    );
}

export default CRGRAllocation;